- if @cart.empty?
  Your shopping cart is empty.
  - if params[:cbed]
    = link_to "Shop Now!", '#', :onclick=>"$('#product-trigger').trigger('click')"
  - else
    = link_to "Shop Now!", market_path


- else
  - if params[:seller]
    - seller = User.find(params[:seller])
    = render :partial=>'cart_per_seller', :locals=>{:cart=>@cart[seller], :seller=>seller}
  - else
    = render 'order_table'