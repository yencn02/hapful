.image_holder
  = link_to image_tag(product.images.first.image(:thumbnail)), product
  .image_title
    = link_to product.name, product
    %br
    by
    %span
      = link_to product.seller.email, ''